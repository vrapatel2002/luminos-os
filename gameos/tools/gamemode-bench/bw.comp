#version 450
layout(local_size_x = 256) in;
layout(std430, binding = 0) buffer B { vec4 data[]; };
layout(push_constant) uniform P { uint iters; } pc;
void main() {
  uint i = gl_GlobalInvocationID.x;
  data[i] = data[i] * 1.0001 + vec4(0.5);
}
