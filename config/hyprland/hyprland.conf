# Luminos Hyprland Configuration
# Auto-generated for Arch Linux + ASUS ROG G14
# Managed by luminos-os — do not edit manually
# Regenerate: python3 -c "from compositor.compositor_config import write_config; write_config()"

# --- Monitor (G14 internal display) ---
monitor = eDP-2, preferred, auto, 2

# --- Environment ---
env = XCURSOR_SIZE, 24
env = XCURSOR_THEME, WhiteSur-cursors
env = HYPRCURSOR_SIZE, 24
env = GTK_THEME, WhiteSur-Dark
env = GDK_BACKEND, wayland
env = QT_QPA_PLATFORM, wayland
env = SDL_VIDEODRIVER, wayland
env = CLUTTER_BACKEND, wayland

# --- Autostart ---
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_RUNTIME_DIR
exec-once = dbus-update-activation-environment --systemd --all
exec-once = /usr/local/bin/luminos-session
exec-once = swww-daemon
exec-once = bash -c 'sleep 1 && swww img ~/Pictures/wallpapers/dark.png --transition-type fade --transition-duration 1'
exec-once = hyprpanel
exec-once = nm-applet --indicator
exec-once = hyprpm reload # [CHANGE: gemini-cli | 2026-04-19] Load Hyprland plugins
exec-once = asusctl aura effect static -c ffffff # [CHANGE: gemini-cli | 2026-04-19] Set keyboard backlight to white static

# --- Input ---
input {
    kb_layout = us
    follow_mouse = 1
    sensitivity = 0

    touchpad {
        natural_scroll = true
        tap-to-click = true
        disable_while_typing = true
        scroll_factor = 0.8
    }
}


# --- Appearance ---
general {
    gaps_in = 4
    gaps_out = 8
    border_size = 2 # [CHANGE: gemini-cli | 2026-04-19] Increased border size
    col.active_border = rgba(0080FFaa)
    col.inactive_border = rgba(ffffff10)
    layout = dwindle
    resize_on_border = true
    allow_tearing = false
}

decoration {
    rounding = 12

    blur {
        enabled = yes
        size = 6
        passes = 2
    }

    shadow {
        enabled = yes
        range = 20
        render_power = 3
        color = rgba(00000066)
    }

    active_opacity = 1.0
    inactive_opacity = 1.0
    dim_inactive = false
}

animations {
    enabled = yes

    bezier = macOS,    0.25, 0.1, 0.25, 1.0
    bezier = macosOut, 0.0,  0.0, 0.58, 1.0

    animation = windows,    1, 3, macOS,    popin 95%
    animation = windowsOut, 1, 3, macosOut, popin 95%
    animation = fade,       1, 3, macOS
    animation = workspaces, 1, 4, macOS,    slide
    animation = layers,     1, 3, macOS,    fade
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
}

# [CHANGE: gemini-cli | 2026-04-19] Removed hyprbars plugin to fix CSD buttons

# --- Layer Rules (floating taskbar) ---
layerrule {
    name = blur-hyprpanel
    match:namespace = ^com\.c0dedbad\.hyprpanel\.client$
    blur = true
    ignore_alpha = 0.3
} # [CHANGE: gemini-cli | 2026-04-19] Migrated layerrule to block-style
layerrule {
    name = interactivity
    match:namespace = ^com\.c0dedbad\.hyprpanel\.client$
} # [CHANGE: gemini-cli | 2026-04-19] Fix namespace and syntax

# --- Window Rules ---

# --- Keybindings ---
$mod = SUPER
$terminal = kitty

bind = $mod, Return,    exec, kitty
bind = SUPER, Q,         killactive # [CHANGE: gemini-cli | 2026-04-19]
bind = ALT, F4,          killactive # [CHANGE: gemini-cli | 2026-04-19]
bind = $mod, V,         togglefloating
bind = SUPER, F,         fullscreen # [CHANGE: gemini-cli | 2026-04-19]
bind = SUPER, M,         fullscreen, 1 # [CHANGE: gemini-cli | 2026-04-19]
bind = $mod, E,         exec, dolphin
bind = $mod SHIFT, E,   exit
bind = $mod SHIFT, R,   exec, hyprctl reload

# Workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Focus movement
bind = $mod, left,  movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up,    movefocus, u
bind = $mod, down,  movefocus, d

# Window movement
bind = $mod SHIFT, left,  movewindow, l
bind = $mod SHIFT, right, movewindow, r
bind = $mod SHIFT, up,    movewindow, u
bind = $mod SHIFT, down,  movewindow, d

# Move/resize with mouse
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Scroll through workspaces
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up,   workspace, e-1

# --- Wallpaper Switcher ---
bind = $mod SHIFT, D, exec, swww img ~/Pictures/wallpapers/dark.png --transition-type fade --transition-duration 1
bind = $mod SHIFT, L, exec, swww img ~/Pictures/wallpapers/light.png --transition-type fade --transition-duration 1

# --- Luminos UI Bindings ---
# Quick Settings panel (Super+S)
bind = $mod, S, exec, luminos-quick-settings-toggle

# App Launcher (Super+Space)
bind = $mod, Space, exec, pkill wofi || wofi --show drun --prompt "Search apps..." --width 600 --height 400 --location center --allow-images --insensitive

# --- Media & Hardware Keys ---
bindel = , XF86AudioRaiseVolume,  exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = , XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = , XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = , XF86MonBrightnessUp,   exec, brightnessctl -e4 -n2 set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

bindl = , XF86AudioNext,  exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay,  exec, playerctl play-pause
bindl = , XF86AudioPrev,  exec, playerctl previous
bindel = , XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = , XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = , XF86MonBrightnessUp,   exec, brightnessctl -e4 -n2 set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

bindl = , XF86AudioNext,  exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay,  exec, playerctl play-pause
bindl = , XF86AudioPrev,  exec, playerctl previous
