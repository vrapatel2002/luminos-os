# Luminos Media Server — Status

Headless media server on a **Dell Inspiron 3590**, separate machine from the G14.
Reached over SSH: `ssh -i ~/.ssh/luminos-server shawn@192.168.2.61`

See [`README.md`](README.md) for the map, [`DECISIONS.md`](DECISIONS.md) for why things
are the way they are, and [`docs/`](docs/) for the runbook, plan and security notes.

## Components

| Component | Status | Notes |
|---|---|---|
| Server hardware | ✅ Settled | [CHANGE: claude-code \| 2026-07-29] Friend's laptop: **i5-10210U, 10th gen Comet Lake, 4c/8t, 16 GB RAM**, SSD + HDD. Nothing purchased. **The SSD is off limits** — the owner's Windows install stays byte-identical, so Arch lives entirely on the HDD including its own EFI partition. UHD 620 (Gen9.5) is spec'd for 4K HEVC 10-bit decode + H264/HEVC encode but has **no AV1 at all**; the Roku can't do AV1 either, so keep AV1 out of the library rather than transcoding it on both ends. Spec-derived — confirm with `vainfo` on first boot. |
| Stage 1 installer | ✅ Written, dry-run tested | [CHANGE: claude-code \| 2026-07-29] `server/scripts/luminos-server-install`. Runs from the booted Arch ISO over SSH. **Detects** the target disk (non-removable + rotational + not USB) instead of assuming `/dev/sda`, aborts if 0 or >1 candidates, and requires `--confirm-disk` to match. Defaults to `--dry-run`. Ends in 12 assertions and refuses to claim success if any fail. No `\|\| true` anywhere. Testing caught a real bug: the original positional `lsblk` parse broke because **`TRAN` is empty for a plain SATA disk**, shifting every field left — it would have found no HDD on the real machine. Now reads each attribute individually via sysfs. |
| Stage 2 services | ✅ Written, dry-run tested | [CHANGE: claude-code \| 2026-07-29] `server/scripts/luminos-server-services`. Jellyfin + **`qbittorrent-nox`** (the GUI package only runs inside a logged-in desktop session — fatal on a headless box) + `sonarr-bin` + `prowlarr-bin` (the `-bin` variants skip a ~1 h .NET build on a 15 W CPU writing to a spinning disk). Single shared group `media` gid 945 pinned to match the G14; `/srv/media/{downloads,tv,movies,.import-staging}` at `2775` setgid. `informant` blocks `pacman -Syu` until Arch news is read, since updates are manual by choice. **Refuses to run on any host not named `luminos-server`**, so it cannot be fired at the G14 by accident. Verification *proves* the permission design by writing as `sonarr` and reading as `jellyfin`, and checks all four ports are bound. Reports `vainfo` output rather than claiming hwaccel works. |
| Install USB | ✅ Written + verified | [CHANGE: claude-code \| 2026-07-29] `archlinux-2026.07.01` written to `/dev/sda` (SanDisk Ultra 57.3 G, `TRAN=usb`, `RM=1`); internal `nvme0n1` never touched. **Proven, not assumed:** read the first 1583022080 bytes back off the raw device and the sha256 matched the ISO exactly (`e86295dc…a6c0`). `EFI/BOOT/BOOTx64.EFI` confirmed present on `ARCHISO_EFI` — the fallback path Dell firmware looks for on removable media. The movie that was on the stick still exists twice under `/srv/media`. |
| Server OS install | ✅ Installed + running | [CHANGE: claude-code \| 2026-07-31] Built on the **Dell Inspiron 3590** (i5-10210U, battery removed so it is AC-only), Arch on the HDD, headless, administered over SSH as `ssh -i ~/.ssh/luminos-server shawn@192.168.2.61`. Original build notes below still apply. [CHANGE: claude-code \| 2026-07-29] Laptop is a **Dell** — `F2` setup, `F12` boot menu. **BitLocker cleared:** protection off on the SSD (`C:`), so the TPM-seal hazard is gone. **HDD cleared:** it *had* been encrypted; owner reformatted it, which freed it. Its 2 existing partitions need no merging — the installer `wipefs`/`sgdisk --zap-all`s the whole table. Remaining warnings in MEDIA_SERVER_PLAN §7a-7c: (1) **never** flip `SATA Operation` from `RAID On` to `AHCI` — stops Windows booting; (2) Secure Boot must go off and **stays** off, which costs nothing for Windows itself but **breaks kernel-anti-cheat games** (Valorant/Fortnite/Battlefield on Win11) — ask the owner first; (3) full shutdown, not Restart, or Fast Startup leaves filesystems half-mounted. |
| Dual-boot design | ✅ Decided | [CHANGE: claude-code \| 2026-07-29] **Two independent bootloaders, zero shared state** — the HDD gets its own ESP and systemd-boot; Windows' bootloader on the SSD is never opened for writing. No GRUB os-prober, nothing that can clobber Windows. OS choice is the `F12` menu. **The HDD must be first in BIOS boot order**, or a power blip brings the box up in Windows at a login screen where SSH is unreachable. Consequence to accept: while Windows is running the media server is offline. Watch for Windows updates reasserting themselves first in NVRAM boot order — that presents as "the server stopped answering", not as a Linux fault. |
| State migration | ✅ Done | [CHANGE: claude-code \| 2026-07-31] `/var/lib/{jellyfin,sonarr,prowlarr}` + `/etc/jellyfin` rsync'd off the G14; library, indexers and watch state carried over rather than rebuilt. Radarr added on the server (it was never on the G14). Direct play to the Roku proven end to end. |
| Jellyfin hardware transcoding | ✅ On + proven | [CHANGE: claude-code \| 2026-08-02] DECISION 37. Was `HardwareAccelerationType: none` — every transcode ran on the CPU of a box that also seeds torrents. Now **VAAPI on `/dev/dri/renderD128`** (Intel CometLake-U UHD, driver **iHD 26.1.5**). **The render-node number does not transfer between machines:** on the G14 `renderD128` is the useless NVIDIA node, but here it is the Intel one and `renderD129` is the AMD Radeon 520 — always resolve it with `ls -l /sys/class/drm/renderD12*/device/driver`. `HardwareDecodingCodecs` set to the list `vainfo` actually reported (`h264, hevc, mpeg2video, vc1, vp8, vp9`); **no AV1** on this silicon, as the hardware spec predicted. Applied via `POST /System/Configuration/encoding`, **not** by editing `/etc/jellyfin/encoding.xml` — Jellyfin rewrites that file on shutdown and would have dropped a hand edit; verified in both the API readback and the on-disk XML. **Measured on real 4K HEVC 10-bit:** CPU `libx264` **1.22x** realtime (barely keeping up — one hiccup from stuttering) vs `h264_vaapi` **4.8x**. **Negative-tested:** the same command on `renderD129` dies with `radeonsi_drv_video.so init failed` / `Device creation failed: -5`, so 4.8x was genuinely the GPU and not a silent software fallback. Tonemapping left off (needs `intel-compute-runtime`); HEVC encode left off (Roku takes H264). |
| Networking — adapter split | ✅ Split done, cable still pending | [CHANGE: claude-code \| 2026-07-31] The box answers on **both** wlan0 `192.168.2.61` and enp2s0 `192.168.2.62`, same subnet, and the ethernet cable is bad so the wire only negotiates **100 Mb/s (≈12.5 MB/s)**. Two separate bugs stacked: qBittorrent's own UPnP had mapped the router's forward to **.62**, and Linux **ARP flux** (default `arp_ignore=0`) let the router learn .61 at the *ethernet* MAC — so traffic addressed to the wifi IP still arrived over the slow wire. `ip route get` said wlan0 the whole time; only `/sys/class/net/<if>/statistics/rx_bytes` and `tcpdump -i <if>` showed the truth (27 k peer packets on enp2s0). Fixed: `/etc/sysctl.d/30-luminos-arp-flux.conf` (`arp_ignore=1`, `arp_announce=2`) + forward re-pointed at .61. Peer split went 15/679 → 14/20, so it is better but **not** fully on wifi. **Real fix is a new ethernet cable** (owner-only), then set `RouteMetric` in `/etc/systemd/network/20-wired.network`. **Superseded by DECISION 36:** torrents now run on the wire (`enp2s0`) via a policy-routing table, leaving the radio for the TV — TV latency under full load went 256 ms/12.5% loss → **8.4 ms/0% loss**. The old warning *'never pin `current_network_interface`'* was wrong: the setting was fine, **not restarting after it** was the bug. Still owner-only: a new cable, plus a **DHCP reservation for .62**. **Corrected by DECISION 36a [CHANGE: claude-code \| 2026-08-02]:** the policy rule is **not** what holds the split — qBittorrent's `current_network_interface=enp2s0` is an `SO_BINDTODEVICE` bind that pins the exit interface at the socket layer and overrides routing entirely. Proven: the rule was silently flushed on 2026-07-31 when the cable flapped (networkd drops rules it does not own, and `networkctl reload` was never run), yet 2 days later traffic was still **enp2s0 9.80 MB/s / wlan0 0.00 MB/s** with 20 peer sockets on .62. So the DHCP reservation is *less* critical than stated and the device binding is the thing to never clear. Rule re-added live. **Also spotted:** 48 × `wlan0: Lost carrier` + lease loss in the 2 days to 08-01 (0 on 08-02) — always reconnects and always regains .61, but each is a ~1 s gap the TV would feel; cause unestablished. |
| BitTorrent port 25989 | ✅ Open on purpose + durable | [CHANGE: claude-code \| 2026-07-31] DECISION 35. Client had uploaded **0 bytes ever** and could only dial out, so 4K swarms stalled at 1 connection. `/etc/nftables.conf` now accepts tcp+udp 25989 from anywhere; every admin surface (WebUI 8080, Jellyfin 8096, Sonarr 8989, Radarr 7878, Prowlarr 9696, ssh) stays LAN-only. Loaded behind a `systemd-run --on-active=180` auto-rollback, cancelled only after SSH survived. Router forward is kept alive by **`qbt-portmap.service` + `.timer`** (boot+90 s, hourly) because qBittorrent's own UPnP stopped mapping entirely. Proven **outside-in** with a throwaway port and a genuinely external fetcher — an online port checker said CLOSED and was simply **wrong**, and probing your own public IP from inside the LAN is meaningless (hairpin NAT). Rationale + how to close it again: `server/docs/MEDIA_SERVER_SECURITY.md` §2a/§2b. |
| 1337x via Byparr | ✅ Working end to end | [CHANGE: claude-code \| 2026-07-30] Byparr/Camoufox clears Cloudflare headless with no virtual display (do **not** load the `playwright_captcha` addon — it is itself fingerprinted). Search still returned interstitials until `/usr/lib/byparr/src/endpoints.py` was patched: the wait loop broke as soon as `page.title()` changed, but a challenge navigates several times and the title is momentarily empty or the next document's between hops. New `_still_challenged()` also requires `__cf_chl` to be gone from `page.url`. Backup at `endpoints.py.orig`. Wired as Prowlarr proxy id 2 + indexer id 10 (tag `flaresolverr`), synced to Sonarr id 6; 16 results on re-verify. |
| True Detective S01-S03 | ✅ Cause found, downloading | [CHANGE: claude-code \| 2026-07-31] Seasons 1-3 showed **0 files** while S4 had 5. Not a bug in the chain — **no 4K version of S01-S03 exists anywhere.** A real Sonarr release search returned **733 releases across the three seasons and not one 2160p** (HBO finished them before 4K streaming and never remastered). The series sat on profile 5 `Ultra-HD`, which allows **only** 2160p, so Sonarr rejected all 733 and grabbed nothing — **silently, forever**. Moved to profile 7 (`Max Bitrate`, cutoff 21, `upgradeAllowed`). Top pick is now **Bluray-1080p Remux** season packs (~90 GB each, untouched disc video). Real seeders **19 / 44 / 70** — verified via `/api/v2/torrents/trackers`, not the indexer's claim. **The general trap: a strict quality profile fails closed and says nothing.** If a season is empty, search it before assuming the download chain is broken. |
| Torrenting | ⛔ **HALTED until a VPN is installed** | [CHANGE: claude-code \| 2026-08-04] DECISION 42, security brief §6a. **No download and no upload, on purpose.** BitTorrent is unencrypted and identifiable by protocol signature, so the non-standard port hides nothing; and because DECISION 35 opened an *inbound* port the box was an **advertised** peer — in the tracker list and in DHT against **76.64.36.43**, where anyone can enumerate it. It had uploaded **228.9 GB** against 389.9 GB down, and seeding is the conspicuous half. **Enforced at four layers** so undoing one does not resume traffic: all 22 torrents `stopped`; `qbittorrent-nox@shawn.service` stopped **and disabled** (so a reboot does not restart it); **`qbt-portmap.timer` stopped and disabled** — easy to miss, because the thing holding the router forward open is a *separate unit from the service it serves*, and it was still armed to fire 15 minutes later; Sonarr+Radarr `rssSyncInterval` → **0** so no grab backlog piles up behind the halt (was 15 and 30 — recorded for restore). **Proven with a control:** `192.168.2.62:25989` and `:8080` both refuse, while `:8096` still answers — without that third probe the first two would prove nothing. nftables rules for 25989 **left in place deliberately**; they are inert with nothing listening, and removing them is a real DECISION 35 reversal, not a side effect. Jellyfin untouched — LAN-only, never crosses the ISP. **Blocked on the user: a VPN provider is a paid subscription and a trust decision.** Then three things must be true before restart — the tunnel must exit `enp2s0` with qBittorrent bound to the tunnel (or torrents ride the radio and the TV stutters again, DECISION 36), the kill-switch must be *tested* by downing the interface mid-transfer, and provider port-forwarding decides whether seeding works at all. |
| Subtitles — MobLand | ✅ Fixed + proven | [CHANGE: claude-code \| 2026-08-04] Reported as "subtitles not working". **Not a missing-subtitle problem** — 9 of 10 episodes had English `subrip` tracks embedded all along. The cause was Jellyfin's user setting **`SubtitleMode: Default`**, which means *"only show tracks the file flags as default"* — and **every** MobLand track has `disposition default=0`, as most web releases do. So Jellyfin selected nothing, silently. Confirmed before touching anything: `PlaybackInfo` returned `DefaultSubtitleStreamIndex = None`. Fixed with `SubtitleMode: Always` + `SubtitleLanguagePreference: eng` — now 10/10 auto-select English, re-checked through the same API call. **E07 was the one real gap** (0 subtitle streams in the file); the unimported 2160p FLUX copy in `/srv/media/downloads` has both English tracks and its duration matches within **19 ms**, so the same cut — extracted with `ffmpeg` to sidecars rather than replacing the library file. Named `.en.default.srt` after a first pass had Jellyfin auto-pick the `.en.sdh.srt` hearing-impaired track, out of step with the other nine. **Side effect checked, not assumed:** House of Cards remuxes carry 5 PGS (image) tracks alongside 2 text ones, and PGS would force burn-in — i.e. a full transcode on every play. Jellyfin picks the `subrip` track, so no burn-in. |
| Season cap — 2 per series | ✅ Enforced by timer, negative-tested | [CHANGE: claude-code \| 2026-08-04] DECISION 44. Sonarr has **no maximum-seasons setting** — Monitor is All / Future / Missing / Existing / First / Last / Pilot / None, with nothing between *one* season and *every* season, which on ~12 GB-an-episode remuxes is how 876 GB disappears. `luminos-season-limit` (`server/scripts/`, installed `/usr/local/bin/`) caps every series at **2 monitored seasons**, run daily by `luminos-season-limit.timer` (`Persistent=true` — the box is powered on and off by hand). Two rules do the work, both because the obvious version was wrong: **anything already at or under the limit is left completely alone** (True Detective is on S01+S04 on purpose — those are the two on disk — and a "first N" rule would silently rewrite it to S01+S02 and queue ~90 GB), and **when trimming, seasons with files beat empty ones**, season number only breaking the tie. That second rule came from a **negative test**: the first version, fed True Detective with a third season monitored, planned to keep S01+S02 and **drop S04's 47.46 GB of real files** in favour of an empty season. Sort key is now `(episodeFileCount == 0, seasonNumber)`. It **never deletes** — unmonitoring stops *acquiring*; it reports over-limit seasons that still hold files with a reclaimable total and leaves the call to a human. Dry run by default; `--apply` **re-reads from Sonarr and verifies** each change, exit 2 if not, because an HTTP 202 is not proof. Unit proven by re-monitoring House of Cards S03 on purpose and watching it trim, then re-running for idempotency — starting it against an already-compliant library proves nothing. |
| Disk space | ✅ 382 GB free (was 222 GB) | [CHANGE: claude-code \| 2026-08-04] DECISION 44. **171.83 GB reclaimed.** 131.10 GB of orphans (45 files Sonarr never imported, or imported by *copying* rather than hardlinking) plus House of Cards S03–S06. **Nothing with a second link was ever deleted** — the pass asserted `nlink == 1` and would have aborted on a hardlink, which nearly mattered: two True Detective S01 files looked like duplicates but had **different inodes** (42205243/42205247 vs 50331738/50331739), so Sonarr had copied, not linked. Library re-verified afterwards at 522.81 GB, every file readable. Reversibility bought *before* deleting: `qbt-BT_backup-20260804-193838.tar.gz` (46 entries) and `deleted-orphans-20260804.json` in `/home/shawn`. The 8 dead torrents were then removed **offline** — deleting `<hash>.torrent` + `<hash>.fastresume` from `BT_backup` with the daemon down, 22 entries → 14 — so a future "resume all" cannot re-fetch the 131 GB, and with zero network traffic, which DECISION 42 requires. |
| Torrent throughput | ✅ Diagnosed, swarm-limited | [CHANGE: claude-code \| 2026-07-31] 0 → **10.74 MB/s** after fixing tracker rot (16 of 23 trackers on the original magnets were dead; ngosang `trackers_best` auto-added, `max_connec` 2000/300, 6 active downloads). The network itself does **46.34 MB/s** (Ubuntu ISO), so 46 MB/s is real — just not from these swarms. **Public indexer seeder counts are fiction:** 1337x claimed 346/256/207, `GET /api/v2/torrents/trackers` after adding showed **3/7/1**. Disk 5–37% util, never the bound. |
| Usenet — provider + credentials | ✅ Bought, proven working | [CHANGE: claude-code \| 2026-08-05] DECISION 48. **UsenetServer**, NNTP-over-TLS on **port 563** (563 is the encrypted port; 119 is plaintext and must never be used here). Credentials live in `server/.env`, mode 600, **gitignored** — verified with `git check-ignore -v`, not assumed. Proven from **both** machines by `server/scripts/luminos-usenet-test`, which walks DNS → TCP → TLS → AUTH separately so a failure says *which* one broke, and never prints the password. Result: TLS 1.3, `TLS_AES_256_GCM_SHA384`, 256-bit, Let's Encrypt cert valid to 2026-10-08, `281 ... (No Posting)` — 44 ms from the G14, 51 ms from the Dell. **`check_hostname` stays ON**: a cert that does not match the hostname means something is intercepting, and that is the one failure a privacy job must never shrug off. The account being **read-only** is the point — it structurally cannot upload, which is the half of BitTorrent that got us noticed (DECISION 42). |
| NZBGet | ✅ Installed, running, wired in | [CHANGE: claude-code \| 2026-08-05] DECISION 48. Listening `0.0.0.0:6789`, user `luminos`; LAN-only in practice because nftables is `policy drop` with one `192.168.2.0/24` accept. Set up by `server/scripts/luminos-nzbget-setup`, driven from `.env`, idempotent, and it **reads the config back and compares all 44 keys** rather than trusting the write. That read-back caught three real bugs: (1) **the verifier itself** — Python `\s` matches newlines, so `\s*` after `=` on an empty value (`LockFile=`) ate the line break and grabbed the next line, failing three correctly-written keys; `[ \t]*` is the fix. (2) **`os.makedirs()` creates missing *parents* with the process umask**, not the mode chmod'd onto the leaves — under `sudo` (umask 0077) `/srv/media/usenet` came out `drwx--S---` and nzbget could not traverse into its own download dirs though the leaves were a perfect 2775. (3) **`CertCheck=yes` will not start without `CertStore`** — pointed at `/etc/ssl/certs/ca-certificates.crt`; cert checking stays on, because with it off a spoofed cert is accepted silently. **`systemctl is-active` said `active` while two of those were live in the journal** — active is not working. Downloads land in `/srv/media/usenet/{complete,incomplete}`, kept separate from the torrent dir so the arrival path is obvious. Added to **Sonarr and Radarr as client id=2**, both `testall` → `valid=True` (`id=1` is qBittorrent, `valid=False`, which is DECISION 42 working). |
| Usenet throughput | ✅ Measured, radio-limited | [CHANGE: claude-code \| 2026-08-05] DECISION 48. **11.45 MB/s (92 Mb/s)** — 307 articles, 235.7 MB in 20.6 s on **15 connections**, measured by `server/scripts/luminos-usenet-speed` pulling real article bodies over real TLS connections (there is no synthetic test file on Usenet). That **beats the best BitTorrent ever managed here (10.74 MB/s)** and sits on the 100 Mb/s wifi ceiling. The plan allows **50** connections; raising the count would buy **nothing**, because the QCA9377 radio is the bound, not the provider. Router reports `MaxBitRateDown : 1024000000 bps` — the line is ~1 Gbps. **Only the Cat 6 cable changes this number**, which makes the pending cable worth roughly 10x rather than a nicety. |
| DNS | ✅ Encrypted, proven on the wire | [CHANGE: claude-code \| 2026-08-05] DECISION 48. Was cleartext to `mtrlpq02dnsvp1.srvr.bell.ca` — encrypting downloads while telling the ISP's resolver what you are looking up is theatre. Now **DNS-over-TLS to Quad9**, strict, via `/etc/systemd/resolved.conf.d/luminos-dot.conf`. **The global config alone is not enough: DNS set on a *link* beats DNS set globally in systemd-resolved**, so DHCP kept handing Bell's resolver to `wlan0`/`enp2s0` and lookups left in cleartext while the global file sat there looking correct. Fixed with `UseDNS=false` drop-ins on both `.network` files (drop-ins, so the hand-commented originals stay readable). **`yes`, not `opportunistic`** — opportunistic falls back to plaintext and says nothing. Proven by packet capture: **51 packets total, 0 on port 53, 51 on port 853, all to 9.9.9.9**. Three earlier attempts returned `0 / 0` and would have read as success — all were **broken tests**: `tcpdump` buffers without `-U`; backgrounding it over SSH kills the session (exit 255); and `-w` under `timeout` loses the pcap because SIGTERM does not flush. The result is trustworthy *because* it caught 51 packets. |
| Indexer | ✅ **NZBGeek, bought and proven** | [CHANGE: claude-code \| 2026-08-05] DECISION 48. **The choice was made by availability, not preference: DrunkenSlug registration is closed** — its own page says *"Sorry! The Bar is closed. Please don't contact us asking for invites."* (read through Byparr, since Cloudflare 403s a plain fetch). NZBGeek's register page serves a live terms form, so it is open. Added to **Prowlarr as indexer id=11**, protocol `usenet`, `/indexer/test` → HTTP 200, then `ApplicationIndexerSync` pushed it to **both Sonarr and Radarr** (both were already registered in Prowlarr at `fullSync`, so **one indexer serves both** — no second subscription needed). API key is 32 alphanumeric chars in `server/.env`, verified by reading the file back and checking length/whitespace/quoting rather than trusting the editor's save. **Proven by real searches, not by the test button:** Sonarr on House of Cards S01 returned **693 releases, 491 of them NZBGeek** (all torrent indexers combined: 202), top hit a 315.6 GB S01-06 1080p BluRay pack; Radarr on The Odyssey returned 3 NZBGeek results — all CAM rips, which is the film being a 2026 cinema release, not an indexer fault. **Retention is the news server's job, not the indexer's** — if something old comes back "not found", that is UsenetServer, and changing indexer will not fix it. |
| Remote Jellyfin access | ✅ **Tailscale, connected and verified** | [CHANGE: claude-code | 2026-08-05] DECISION 51. Chose **Tailscale** over self-hosted WireGuard and over a plain port forward. `extra/tailscale 1.98.10-1`, native package, **no Docker**. Both ends dial *out*, so **no router port was opened** — `upnpc -l` still reports zero forwards and the box stays invisible from the internet. Server is `luminos-server` at tailnet IP **100.82.125.26** (MagicDNS `luminos-server.tail1fd435.ts.net`), `tailscaled` enabled + active, backend `Running`. **The trap that would have made this fail silently:** the existing `ip saddr 192.168.2.0/24 accept` rule does *not* match tunnel peers — they arrive on `tailscale0` with `100.64.0.0/10` CGNAT addresses. Added `iifname "tailscale0" accept` to `/etc/nftables.conf` (backups `/etc/nftables.conf.pre-tailscale`, `/root/nft-pre-tailscale.rules`; `nftables.service` enabled so it survives reboot). Tailscale's own accept rules live in the `iptables-nft` filter table and do **not** override our `policy drop`, so ours is the rule doing the work. Brought up with **`--accept-dns=false`**, which is not optional: Tailscale hijacks DNS by default and would have quietly undone the DNS-over-TLS to Quad9 from DECISION 48. Verified against the recorded before-state — global still `+DNSOverTLS` to `9.9.9.9#dns.quad9.net`, `tailscale0` claims no DNS scope, and a live lookup reports *acquired via encrypted transport: yes*. `curl --interface 100.82.125.26 http://100.82.125.26:8096/System/Info/Public` → **200**, proving Jellyfin listens on the tunnel address — but **not** the firewall rule, whose counter is still 0 because loopback traffic never crosses `iifname tailscale0`. That rule is only proven when the first real peer connects. **Owner-only:** the machine key expires **2027-02-01**; when it does the server drops off the tailnet silently, and disabling expiry is a console toggle with no CLI equivalent. |
| Jellyfin web theme | ✅ ElegantFin v26.06.06 applied | [CHANGE: claude-code | 2026-08-05] 73,708 bytes in `CustomCss`, pinned to the tagged release (upstream sha256 `5c2ce809bd58963efce441755d88b109bf139f4f594fc601b1d6ab8ffb7f70ae`), **not** the `@latest` CDN path — jsDelivr 403s this repo for size anyway. The Google Fonts `@import` for Inter was **stripped**: it called `fonts.googleapis.com` on every page load, which is the same class of leak DECISION 48 closed for DNS. Inter is already installed on the G14 so it still renders; a `font-family` line was prepended for the fallback chain. **Two calls to `fonts.gstatic.com` remain**, for the Material Symbols Rounded icon font. Left deliberately: the file is **5.2 MB** so base64-embedding it would put ~7 MB in the branding config, and dropping it into `/usr/share/jellyfin/web/` would be silently erased by the next `jellyfin-web` upgrade and break every icon. Jellyfin ships the *older* MaterialIcons, not Material Symbols, so there is no local substitute. Verified by fetching the unauthenticated `/Branding/Css` endpoint a browser actually hits (HTTP 200, 73,708 bytes, `text/css`), with `/Branding/Nope` → 404 as the negative control, plus 724/724 balanced braces and a clean tail. **This affects the browser only.** The Roku and Android apps never load CSS — they are native and cannot be themed. Revert = clear the Custom CSS box. |
| Media requests — Jellyseerr | ✅ **Built from source, wired, proven with a real request** | [CHANGE: claude-code | 2026-08-05] DECISION 54. **Jellyseerr 3.4.1** on port 5055, signed in with the same Jellyfin account, both libraries linked, Sonarr and Radarr attached on **quality profile id=7**. Answers the "search for a film and download it" ask that **Jellyfin itself cannot do** — its search is not extensible and the Roku/Android clients are native, so a web-only hack would miss the two places the library is actually used. **Built from upstream, not the AUR:** the project renamed to `seerr-team/seerr` and the AUR recipe is pinned to **2.7.3 (2025-08-14)** against upstream **3.4.1 (2026-07-30)**. PKGBUILD vendored at `server/packaging/jellyseerr/PKGBUILD`. Three adaptations: the upstream renames (`seerr-api.yml`, `next.config.ts`); **`engines.node` relaxed to `>=22.19.0`** because the repo's `.npmrc` sets **`engine-strict=true`**, which makes pnpm *hard-refuse* rather than warn — the box runs Node 26.5.0 and `nodejs-lts-jod` conflicts with `nodejs`, which `python-playwright` (Byparr) needs; and **`arch.patch` deliberately not applied** — its csurf hunk rewrites `server.use(csurf({...}))` to `server.use(() => csurf({...}))`, which does not fix CSRF, it **silently disables** it. Watch on the next upgrade: pnpm 11 prints *the "pnpm" field in package.json is no longer read*, so `onlyBuiltDependencies`/`overrides` are ignored — harmless here (`bcrypt@6.0.0`, `sqlite3@5.1.7` both compiled) but the first place to look if a build starts and then dies on a native module. **`is4k` off on purpose:** Jellyseerr's separate 4K path would route requests to a strict profile like id=5 `Ultra-HD`, which is exactly what rejected 733 True Detective releases silently. **No firewall change needed** — the `192.168.2.0/24` and `iifname tailscale0` rules are blanket accepts, so 5055 was reachable the moment it listened, and nothing is open to the internet; the flip side is that *any* new service here is LAN-and-tailnet-exposed by default. **Proven with a real request, not the test button:** `POST /api/v1/request` for Interstellar → 201 → Radarr added it on profile 7 at `/srv/media/movies`, monitored → indexer searched → NZBGet grabbed. Reachability tested **from the G14**, a different machine (HTTP 200 on the API, `GET /` → 200 → `/login`, 298 KB of HTML). **The Tailscale path is untested** — this laptop is not on the tailnet, so a 000 from `100.82.125.26:5055` proves nothing. |
| Download protocol | ✅ **Usenet-only, enforced and negative-tested** | [CHANGE: claude-code \| 2026-08-07] DECISION 59. Torrent closed at **three** layers: qBittorrent client `enable=False` in Sonarr+Radarr, `enableTorrent=False` on both delay profiles, all 9 Prowlarr torrent indexers disabled. `preferredProtocol=usenet` was already set and did **nothing** — preferred means ranked first, not exclusive; that is why MobLand, House of Cards and two Odyssey grabs still went over torrent. **qbittorrent is not installed** (`is-enabled` → `not-found`), so those grabs went to a nonexistent client and failed silently. Proven after the change: sonarr 59 releases/59 usenet, radarr 3/3 usenet, 0 torrent, queue 26 items all usenet. **Prowlarr disables indexers but never deletes them** — Pirate Bay still shows in the UI with all three search flags `False`; that is inert, not live. |
| Subtitle pre-extraction | ⚠️ **Done once, not automated** | [CHANGE: claude-code \| 2026-08-07] DECISION 60. "Subs are gone" was **latency, not absence** — Jellyfin demuxes embedded tracks on demand at first play; extraction ran 144 s while playback was abandoned at 69 s. Timed control: cached 0.0059 s vs uncached >120 s. Jellyfin 10.11.11 has **no option and no scheduled task** for this. `server/scripts/luminos-subtitle-warm` swept the library: 77/78 ok (Interstellar timed out), cache 5 → 290 files. **Still cold for new downloads** — not on a timer yet, and it reads credentials from `/tmp/jftoken` which does not survive reboot. Text codecs only; PGS/VOBSUB untouched. |
| Disk-full guard (NZBGet) | ✅ **Live at 25 GB — was silently off** | [CHANGE: claude-code \| 2026-08-08] Raised from the 250 MB default before starting ~200 GB of 2160p grabs. **The first attempt did nothing and reported nothing:** the line was written `DiskSpace = 25000` with spaces, and NZBGet **silently ignores that form and keeps the default** — the file said 25000, the process confirmed it was reading that exact file, and a restart after the edit still left the live value at **250**. No warning anywhere. Every other line in `nzbget.conf` is `Name=value`; rewritten to match and the live value took effect at once. Verified by reading it back out of the **running process** (`{"method":"config"}` over JSON-RPC), which is the only thing that counts — grepping the config file is what hid this. |
| Descriptive audio | ✅ **Blocked at four layers, audited weekly** | [CHANGE: claude-code \| 2026-08-07] DECISION 61. MobLand played with a narrator describing the picture. **The container flags did not say so** — the real file had `visual_impaired=0` *and* `comment=0`, with only `tags.title = "British (Descriptive)"` giving it away, so any detector keyed on disposition (the documented way) reads it as clean. Detection keys on the **track title**. Four layers: a `-10000` custom format in both apps across all 7 profiles (`minFormatScore=0`, so negative = hard reject); **self-healing** — the bad file scores under `cutoffFormatScore`, which makes any clean release an upgrade, so Sonarr replaced it with no manual delete; Jellyfin `AudioLanguagePreference` pinned to English rather than trusting the default-track flag; and `luminos-audio-audit` weekly. The unit is **allowed to fail** on exit 1 so `systemctl list-units --failed` surfaces it rather than a log nobody reads. **The regex was nearly a bug** — `\bAD\b` would have rejected *Ad Astra*; tightened to need an adjacent quality token and negative-tested on 13 real titles, 0 false positives. Honest note: the offending title was on screen during an earlier audit and went unread — hence a scheduled check rather than a promise to look harder. |
| Library space browser | ✅ **Live on :8099, controls proven end to end** | [CHANGE: claude-code \| 2026-08-08] DECISION 62. `luminos-space`, a Filelight-for-media page in Chrome: size by show / season / episode and by movie, delete at any of those levels, plus live NZBGet control (pause, resume, move to top, cancel, pause-all) with the paused items tagged. **It is not a `du` wrapper because of hardlinks** — Sonarr/Radarr import by hardlink, measured `links=1: 0.0 GB` vs `links>1: 369.9 GB`, so deleting the library name alone frees **nothing**. Twins are matched by `(st_dev, st_ino)`, never by name, snapshotted before the API delete and unlinked after; it reports `frees` separately from apparent size, negative-tested on the case that would make it lie (inode linked outside the deletion set → 0). **Three NZBGet lies handled:** `pausedownload` over GET is a silent no-op (must POST); `PausedSizeMB > 0` is the par2 set, not a pause flag (1.6 of 21.9 GB on a healthy item — the real test is `>= RemainingSizeMB`); and `Status` lags up to **16 s** on the active item — bytes froze at t+4s but `Status` read `DOWNLOADING` until t+16s, so the UI's signal is *more* truthful than NZBGet's own. Every action is read back **before and after**, because `editqueue` reports success for a command that matched nothing — a bogus id now says so instead of looking like a completed delete. Token in the URL, since nftables accepts the LAN and tailnet wholesale. |
| Skip intro / skip credits | ✅ **Intro Skipper 1.10.11.22 active, first segments proven** | [CHANGE: claude-code \| 2026-08-11] DECISION 64. Fingerprints episodes and writes Jellyfin `MediaSegments`; re-runs **daily at 00:00**, so new episodes are covered automatically. Installed by hand — the plugin repo manifest is version-aware and serves nothing to a browser, and the release zip holds **only `IntroSkipper.dll`**, no `meta.json`; Jellyfin loads it anyway and reports `Active`. **Since 10.10 this plugin does not touch the web UI** — it is a Media Segment Provider, so **the Skip button is drawn by the client**, and ask-to-skip vs auto-skip is a per-device setting only the person with the remote can set. Roku supports it ("Skip Segments", per-type "Ask to skip", segments on the progress bar); this server's Roku is 3.2.3, past the 3.1.8 that added it. Depends on **chromaprint in `jellyfin-ffmpeg`**, and Arch is not on the plugin's supported list — checked directly: `jellyfin-ffmpeg 1:7.1.4p1-2` has the muxer and `--enable-chromaprint` (the system `ffmpeg 8.1.2` does not, and is not what Jellyfin runs). It compares episodes **within a season**, so a one-episode season can never produce a result. Verified through the client-facing `GET /MediaSegments/{id}`, not just the DB: Better Call Saul S02E01 intro `340→355s`, credits `2781→2828s`. Scan is **I/O bound on the HDD, not the CPU** — two ffmpeg workers at 15% and 8% — so `MaxParallelism=2` stays. |
| Subtitle fetching (Bazarr) | ❌ Not installed | [CHANGE: claude-code \| 2026-08-07] Nothing on this box can *fetch* subtitles — only what is already inside the release file is available. Blocks the "French audio + English subtitles" request (DECISION 60). Would need Bazarr **and** a French-capable Usenet indexer: a search of the one live indexer returned **0** French/MULTI results for two well-known French titles. |
| Jellyfin streaming path | ⚠️ **Wifi is the bottleneck — awaiting owner decision** | [CHANGE: claude-code \| 2026-08-07] Reported "lagging". Measured, same 250 MB range request of the playing file: wifi `.61` **34.1 / 36.9 Mbit/s**, wired `.62` **89.8 Mbit/s**. The stream needs **32.3 Mbit/s** (h264 direct 32,049,493 bps + DTS-HD MA transcode), so wifi has ~5% headroom on a VBR remux — that is the stutter. Cause is **two radio hops** (server→router→G14) sharing airtime; the 92 Mbit/s Usenet figure was one hop. YouTube being fine proves nothing: ~5 Mbit/s over one hop vs 32 over two. Both radios healthy (-27 dBm server, -64 dBm G14). `enp2s0` is **idle** since torrenting stopped. Moving Jellyfin to the wire **reverses DECISION 36's rationale**, so it needs a decision, not a silent change. Wire negotiates only 100 Mb/s — the bad-cable item is still open. |
| Download size caps | ⚠️ **None exist — accepted deliberately** | [CHANGE: claude-code | 2026-08-05] DECISION 54. Every Radarr quality definition reads **`maxSize: None`**, 1080p and 2160p alike, so profile 7 "Max Bitrate" means literally *the biggest file the indexer offers*. The first Jellyseerr request proved it by grabbing a **103.5 GB** Dolby Vision / TrueHD Atmos remux — ~2.4 hours of the only link at the measured 11.5 MB/s wifi ceiling. **Left alone by choice** (films are watch-then-delete, 382 GB free), but it is a standing property, not a one-off: the same profile is attached to TV season packs. Setting `maxSize` on the quality definitions is the lever if this ever needs to be smaller. **Related trap found while pausing it:** NZBGet's `pausedownload` over **GET returns an empty body and does nothing** — it needs POST, and the error only appears if the response is read. The first pause was a silent no-op while downloading continued at 11.8 MB/s. Proven stopped only by three status samples 12 s apart, all `paused=True`, `0.00 MB/s`, bytes frozen — a single sample cannot tell a pause from a stalled article. |
